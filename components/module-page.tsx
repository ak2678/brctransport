"use client";import {useState}from"react";
export function ModulePage({title,icon,add,empty,fields,summary=[]}:{title:string;icon:string;add:string;empty:string;fields:string[];summary?:string[]}){const [open,setOpen]=useState(false);return <><div className="actions"><div><h1 className="title">{icon} {title}</h1><span className="muted">0 {title.toLowerCase()}</span></div><button className="btn blue" onClick={()=>setOpen(true)}>+ {add}</button></div>{summary.length>0&&<div className="grid">{summary.map(s=><div className="card" key={s}><div className="label">{s}</div><div className="value">₹0</div></div>)}</div>}<div className="tabs"><button className="tab">All</button><button className="tab">My Records</button></div><input className="search" placeholder={`🔍 Search ${title.toLowerCase()}...`}/><section className="panel empty"><div><div style={{fontSize:42}}>{icon}</div><strong>{empty}</strong><p className="muted">Add your first record to get started</p><button className="btn" onClick={()=>setOpen(true)}>+ {add}</button></div></section>{open&&<div className="modal"><form className="dialog" onSubmit={e=>{e.preventDefault();setOpen(false)}}><div className="actions"><h2>+ {add}</h2><button type="button" className="btn alt" onClick={()=>setOpen(false)}>×</button></div><div className="form">{fields.map((f,i)=><div className={i===0?'field wide':'field'} key={f}><label>{f}{i===0?' *':''}</label><input required={i===0} placeholder={f}/></div>)}</div><div className="actions" style={{marginTop:20}}><button type="button" className="btn alt" onClick={()=>setOpen(false)}>Cancel</button><button className="btn">Save</button></div></form></div>}</>}
