import {AppShell}from"@/components/app-shell";export default function Dashboard(){return <AppShell><div className="actions"><div><h1 className="title">Namaste! 🙏</h1><span className="muted">Your operations at a glance</span></div><button className="btn">+ New Trip</button></div><div className="grid">{['Total Trips','Active Trips','Closed','Pending','Party Due','Trans Due','Net Profit','Month Revenue'].map(x=><div className="card" key={x}><div className="label">{x}</div><div className="value">{x.includes('Trip')||x==='Closed'||x==='Pending'?'0':'₹0'}</div><span className="muted">No records yet</span></div>)}</div>{['Revenue vs Profit','⚠️ Payment Alerts','Recent Trips'].map(x=><section className="panel empty" key={x}><div><b>{x}</b><strong>{x==='Revenue vs Profit'?'No data available':x==='Recent Trips'?'No trips yet':'Sab clear!'}</strong><p className="muted">{x==='Recent Trips'?'Create your first trip':'This area updates when business records exist'}</p></div></section>)}</AppShell>}
