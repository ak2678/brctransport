"use client";import Link from"next/link";import{useState}from"react";export default function Login(){const[e,setE]=useState('');return <main className="auth"><div className="tabs"><b>Login</b><Link href="/signup">Sign Up</Link></div><h1>Welcome back!</h1><p className="muted">Login to your BRC account</p><form onSubmit={x=>{x.preventDefault();setE('Authentication will be enabled after the frontend phase.')}}><div className="field"><label>MOBILE NUMBER</label><input required inputMode="numeric" placeholder="+91 10-digit mobile"/></div><div className="field"><label>PASSWORD</label><input required type="password" placeholder="Enter password"/></div><button className="btn" style={{width:'100%',marginTop:18}}>Login →</button></form>{e&&<p className="muted">{e}</p>}<p className="muted">Don&apos;t have an account? <Link href="/signup" style={{color:'#16a34a'}}>Sign Up Free</Link></p></main>}
